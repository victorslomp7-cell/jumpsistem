/**
 * Placeholder até a Fase 1 conectar a um projeto Supabase real.
 * A partir daí, gerar com:
 *   supabase gen types typescript --project-id <id> > src/types/database.types.ts
 */
export type Database = Record<string, unknown>;
